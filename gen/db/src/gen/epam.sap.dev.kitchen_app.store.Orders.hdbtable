-- generated by cds-compiler version 2.5.2
COLUMN TABLE epam_sap_dev_kitchen_app_store_Orders (
  createdAt TIMESTAMP,
  createdBy NVARCHAR(255),
  modifiedAt TIMESTAMP,
  modifiedBy NVARCHAR(255),
  orderUUID NVARCHAR(36) NOT NULL,
  toMarket_marketUUID NVARCHAR(36),
  orderID INTEGER,
  quantity INTEGER,
  calendarYear NVARCHAR(5000),
  deliveryDate DATE,
  orderNetAmount DECIMAL(15, 2),
  orderTaxAmount DECIMAL(15, 2),
  oderGrossAmount DECIMAL(15, 2),
  CurrencyCode_code NVARCHAR(3),
  PRIMARY KEY(orderUUID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN epam_sap_dev_kitchen_app_store_Markets AS toMarket ON (toMarket.marketUUID = toMarket_marketUUID),
  MANY TO ONE JOIN sap_common_Currencies AS CurrencyCode ON (CurrencyCode.code = CurrencyCode_code)
)