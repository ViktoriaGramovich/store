-- generated by cds-compiler version 2.5.2
COLUMN TABLE epam_sap_dev_kitchen_app_store_Products (
  createdAt TIMESTAMP,
  createdBy NVARCHAR(255),
  modifiedAt TIMESTAMP,
  modifiedBy NVARCHAR(255),
  productUUID NVARCHAR(36) NOT NULL,
  productID NVARCHAR(15),
  phase_ID NVARCHAR(5000),
  price DECIMAL(15, 2),
  taxrate INTEGER,
  height DECIMAL(13, 3),
  "DEPTH" DECIMAL(13, 3),
  width DECIMAL(13, 3),
  supplier_ID INTEGER,
  measure_msehi NVARCHAR(3),
  CurrencyCode_code NVARCHAR(3),
  productNetAmount DECIMAL(15, 2),
  productTaxAmount DECIMAL(15, 2),
  productGrossAmount DECIMAL(15, 2),
  productTotalQuantity INTEGER,
  toProductGroup_ID NVARCHAR(3),
  PRIMARY KEY(productUUID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN masterdata_Phases AS phase ON (phase.ID = phase_ID),
  MANY TO ONE JOIN masterdata_UnitOfMeasure AS measure ON (measure.msehi = measure_msehi),
  MANY TO ONE JOIN sap_common_Currencies AS CurrencyCode ON (CurrencyCode.code = CurrencyCode_code),
  MANY TO ONE JOIN masterdata_ProductGroups AS toProductGroup ON (toProductGroup.ID = toProductGroup_ID),
  MANY TO MANY JOIN epam_sap_dev_kitchen_app_store_Markets AS market ON (market.toProduct_productUUID = productUUID)
)