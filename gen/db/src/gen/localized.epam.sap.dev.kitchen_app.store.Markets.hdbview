-- generated by cds-compiler version 2.5.2
VIEW localized_epam_sap_dev_kitchen_app_store_Markets AS SELECT
  L.createdAt,
  L.createdBy,
  L.modifiedAt,
  L.modifiedBy,
  L.marketUUID,
  L.toMarketInfos_ID,
  L.toProduct_productUUID,
  L.startDate,
  L.endDate,
  L.marketStatus,
  L.marketNetAmount,
  L.marketTaxAmount,
  L.marketGrossAmount,
  L.marketTotalQuantity,
  L.CurrencyCode_code
FROM epam_sap_dev_kitchen_app_store_Markets AS L
WITH ASSOCIATIONS (
  MANY TO ONE JOIN masterdata_MarketInfos AS toMarketInfos ON (toMarketInfos.ID = toMarketInfos_ID),
  MANY TO ONE JOIN localized_epam_sap_dev_kitchen_app_store_Products AS toProduct ON (toProduct.productUUID = toProduct_productUUID),
  MANY TO ONE JOIN localized_sap_common_Currencies AS CurrencyCode ON (CurrencyCode.code = CurrencyCode_code),
  MANY TO MANY JOIN localized_epam_sap_dev_kitchen_app_store_Orders AS "ORDER" ON ("ORDER".toMarket_marketUUID = marketUUID)
)