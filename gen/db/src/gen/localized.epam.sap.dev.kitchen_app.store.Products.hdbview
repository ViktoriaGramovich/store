-- generated by cds-compiler version 2.5.2
VIEW localized_epam_sap_dev_kitchen_app_store_Products AS SELECT
  L.createdAt,
  L.createdBy,
  L.modifiedAt,
  L.modifiedBy,
  L.productUUID,
  L.productID,
  L.phase_ID,
  L.price,
  L.taxrate,
  L.height,
  L."DEPTH",
  L.width,
  L.supplier_ID,
  L.measure_msehi,
  L.CurrencyCode_code,
  L.productNetAmount,
  L.productTaxAmount,
  L.productGrossAmount,
  L.productTotalQuantity,
  L.toProductGroup_ID
FROM epam_sap_dev_kitchen_app_store_Products AS L
WITH ASSOCIATIONS (
  MANY TO ONE JOIN masterdata_Phases AS phase ON (phase.ID = phase_ID),
  MANY TO ONE JOIN masterdata_UnitOfMeasure AS measure ON (measure.msehi = measure_msehi),
  MANY TO ONE JOIN localized_sap_common_Currencies AS CurrencyCode ON (CurrencyCode.code = CurrencyCode_code),
  MANY TO ONE JOIN masterdata_ProductGroups AS toProductGroup ON (toProductGroup.ID = toProductGroup_ID),
  MANY TO MANY JOIN localized_epam_sap_dev_kitchen_app_store_Markets AS market ON (market.toProduct_productUUID = productUUID)
)